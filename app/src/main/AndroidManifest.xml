<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.brianofrim.juqe">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".venue.CodeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".user.UserView" />


        <activity
            android:name=".songs.SongPoolActivity"
            android:label="@string/title_activity_song_pool"></activity>

        <activity android:name=".venue.DjActivity" />
        <activity android:name=".venue.VenueSignupActivity"></activity>

    </application>

</manifest>